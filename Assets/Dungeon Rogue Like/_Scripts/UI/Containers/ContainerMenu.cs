using UnityEngine;

namespace Dungeon.UI.Containers
{
    public class ContainerMenu : MonoBehaviour
    {
        //[Header("Container UI Renderer")]
        //[Space(20)]
        //[SerializeField] private ContainerCell _containerCellPrefab;
        //[SerializeField] private Transform _itemsContainer;

        //private Container _container;

        //public void Show(Container container)
        //{
        //    gameObject.SetActive(true);
        //    _container = container;
        //    Render(_container);
        //}

        //public void Hide()
        //{
        //    gameObject.SetActive(false);
        //}

        //private void Render(Container container)
        //{
        //    for (int i = 0; i < container.Pool.Count; i++)
        //    {
        //        var cell = Instantiate(_containerCellPrefab, _itemsContainer);
        //        cell.Render(_container.Pool.GetFirst());
        //    }
        //}
    }
}
